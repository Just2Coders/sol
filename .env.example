# Connection string de Neon (Dashboard → Connect → Connection string)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Secreto para firmar las cookies de sesión (genera uno con: openssl rand -base64 32)
SESSION_SECRET="cambiame"

# Dominio público del sitio: base de las URLs canónicas y de Open Graph.
# En local puede omitirse (cae a http://localhost:3000).
NEXT_PUBLIC_SITE_URL="https://solaris.example"

# Vercel Blob — almacenamiento de las imágenes de productos y kits.
# Vercel lo inyecta solo en sus entornos; para desarrollo local, cópialo de
# Vercel → Storage → tu store → .env.local  (ver docs/DEPLOYMENT.md).
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Secreto del cron de inventario (genera uno con: openssl rand -base64 32).
# Vercel manda `Authorization: Bearer $CRON_SECRET` al invocar /api/cron si la
# variable se llama exactamente así. Sin ella el endpoint responde 503 y el
# barrido no corre: falla cerrado, porque una ruta que muta inventario sin
# autenticar es peor que un cron que no se ejecuta.
CRON_SECRET="cambiame"
